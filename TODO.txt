PHPMYGPX
- Externalize all the SQL sentences from the code to an object that creates the
queries with the given parameters
- Convert the application to use DB2 instead of mysql. Con la versión 9.7.4 es
más fácil porque hay tablas sin esquema.
- Incorporate a add-in such as the DB2 spatial extender to search photos
according to space criteria, not only position.
- To have an option to show only the desired options, not everything (not
traces, etc.)

